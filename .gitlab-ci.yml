include:
  - remote: 'https://gitlab.com/cscs-ci/recipes/-/raw/master/templates/v2/.ci-ext-devel.yml'

stages:
  - run

test:
  extends: .uenv-runner-eiger-mc
  tags: [f7t-runner-tds]
  stage: run
  script:
    - echo test
  variables:
    CSCS_RUNNER_MODE: 'uenv-builder'
    RECIPE_PATH: uenv_recipes/prgenv-gnu
