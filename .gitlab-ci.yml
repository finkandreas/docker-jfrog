include:
  - remote: 'https://gitlab.com/cscs-ci/recipes/-/raw/master/templates/v2/.ci-ext.yml'

stages:
  - reframe-run

run reframe:
    extends: .reframe-runner-daint
    stage: reframe-run
    timeout: 8h
    variables:
      FIRECREST_BASEDIR: /scratch/snx3000/jenkssl/reframe-runner/$CI_JOB_ID
      RFM_FIRECREST: '1'
      RFM_CONFIG: config/cscs.py
      RFM_CHECKPATH: checks/prgenv
      SLURM_PARTITION: debug
